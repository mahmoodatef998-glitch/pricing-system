# ⚡ دليل سريع للنشر - Quick Deployment Guide

**للذين يريدون النشر بسرعة!**

---

## 🚀 10 خطوات سريعة

### 1️⃣ شراء Domain
- Namecheap أو Cloudflare
- $10-15/سنة

### 2️⃣ إنشاء Hosting Server
- DigitalOcean ($6/شهر)
- Ubuntu 22.04 LTS
- 1GB RAM

### 3️⃣ الاتصال بـ Server
```bash
ssh root@[SERVER_IP]
```

### 4️⃣ تثبيت Docker
```bash
curl -fsSL https://get.docker.com -o get-docker.sh
sh get-docker.sh
apt install docker-compose -y
```

### 5️⃣ رفع المشروع
```bash
# من جهازك (PowerShell)
scp -r "C:\Users\admin\Desktop\mahmood\pricing system" root@[SERVER_IP]:/opt/
```

### 6️⃣ إعداد Environment Variables
```bash
cd /opt/pricing-system
nano .env.production
# أدخل القيم من .env.production.example
```

### 7️⃣ إعداد DNS
- في لوحة Domain: أضف A Record → [SERVER_IP]
- انتظر 5-30 دقيقة

### 8️⃣ تثبيت Nginx + SSL
```bash
apt install nginx certbot python3-certbot-nginx -y
certbot --nginx -d your-domain.com
```

### 9️⃣ تشغيل المشروع
```bash
cd /opt/pricing-system
docker-compose -f docker-compose.prod.yml --env-file .env.production up -d
```

### 🔟 اختبار
- افتح: `https://your-domain.com`
- سجل دخول: admin / [YOUR_PASSWORD]

---

## ✅ تم!

**الوقت المتوقع:** 2-3 ساعات

**التكلفة:** ~$16-27 (الشهر الأول)

---

**للتوضيح الكامل:** راجع `دليل_النشر_على_HOSTING_و_DOMAIN.md`



