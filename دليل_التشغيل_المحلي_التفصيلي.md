# 🖥️ دليل التشغيل المحلي التفصيلي - Step by Step Guide

**التاريخ:** 19 نوفمبر 2025  
**الهدف:** شرح تفصيلي كامل لتشغيل المشروع محلياً

---

## 📋 المتطلبات الأساسية

### 1. متطلبات النظام:
- ✅ **Windows 10/11** (أو Mac/Linux)
- ✅ **RAM:** 4GB على الأقل (8GB موصى به)
- ✅ **Storage:** 10GB مساحة فارغة
- ✅ **معالج:** أي معالج حديث

### 2. البرامج المطلوبة:
- ✅ **Docker Desktop** (الأهم!)
- ✅ **متصفح** (Chrome, Firefox, Edge)

---

## 📥 الخطوة 1: تثبيت Docker Desktop

### للـ Windows:

1. **تحميل Docker Desktop:**
   - اذهب إلى: https://www.docker.com/products/docker-desktop
   - اضغط "Download for Windows"
   - حجم الملف: ~500MB

2. **تثبيت Docker Desktop:**
   - شغّل الملف المحمّل
   - اتبع التعليمات (Next, Next, Install)
   - قد يحتاج إعادة تشغيل الكمبيوتر

3. **تشغيل Docker Desktop:**
   - بعد التثبيت، افتح Docker Desktop من قائمة Start
   - انتظر حتى يظهر "Docker Desktop is running"
   - قد يستغرق 1-2 دقيقة في المرة الأولى

4. **التحقق من التثبيت:**
   ```bash
   # افتح Command Prompt أو PowerShell
   docker --version
   # يجب أن يظهر: Docker version 20.x.x
   
   docker-compose --version
   # يجب أن يظهر: Docker Compose version v2.x.x
   ```

---

## 📁 الخطوة 2: إعداد المشروع

### 1. التأكد من وجود المشروع:
```
C:\Users\admin\Desktop\mahmood\pricing system
```

### 2. فتح مجلد المشروع:
- افتح File Explorer
- اذهب إلى: `C:\Users\admin\Desktop\mahmood\pricing system`

### 3. التحقق من الملفات:
يجب أن تجد:
- ✅ `docker-compose.yml`
- ✅ `start.bat`
- ✅ `start-local-network.bat`
- ✅ مجلد `backend/`
- ✅ مجلد `frontend/`

---

## 🚀 الخطوة 3: تشغيل المشروع (الطريقة السهلة)

### الطريقة 1: استخدام `start-local-network.bat` (موصى به)

1. **افتح مجلد المشروع:**
   ```
   C:\Users\admin\Desktop\mahmood\pricing system
   ```

2. **اضغط مرتين على:**
   ```
   start-local-network.bat
   ```

3. **انتظر حتى يكتمل:**
   - سيظهر نافذة Command Prompt
   - سترى رسائل مثل:
     ```
     Starting services...
     Waiting for services to be ready...
     Local IP Address: 192.168.1.100
     ```

4. **عند الانتهاء:**
   - ستظهر رسالة "✅ Services Started!"
   - سترى IP Address الخاص بجهازك
   - مثال: `http://192.168.1.100:3001`

---

### الطريقة 2: استخدام `start.bat` (للاختبار المحلي)

1. **اضغط مرتين على:**
   ```
   start.bat
   ```

2. **الفرق:**
   - `start.bat`: يعمل على `localhost` فقط (من نفس الجهاز)
   - `start-local-network.bat`: يعمل على الشبكة (من أي جهاز)

---

## 🔥 الخطوة 4: فتح Firewall (مرة واحدة فقط)

### لماذا؟
- للسماح بالوصول من أجهزة أخرى في نفس الشبكة
- إذا استخدمت `start.bat` فقط، لا تحتاج هذه الخطوة

### الطريقة:

1. **كـ Administrator:**
   - اضغط بزر الماوس الأيمن على `start-local-network-firewall.bat`
   - اختر "Run as administrator"

2. **انتظر حتى يكتمل:**
   - سيظهر:
     ```
     ✅ Port 3001 opened (Frontend)
     ✅ Port 4000 opened (Backend)
     ✅ Port 8081 opened (Database Admin)
     ```

3. **تم!** - لن تحتاج تكرار هذه الخطوة

---

## 🌐 الخطوة 5: الوصول إلى المشروع

### من نفس الجهاز (Localhost):

افتح المتصفح واذهب إلى:

1. **Frontend (واجهة المستخدم):**
   ```
   http://localhost:3001
   ```

2. **Backend API:**
   ```
   http://localhost:4000/health
   ```

3. **Database Admin (Adminer):**
   ```
   http://localhost:8081
   ```

---

### من أجهزة أخرى في نفس الشبكة:

1. **اعرف IP Address:**
   - من نافذة `start-local-network.bat`
   - أو من Command Prompt:
     ```bash
     ipconfig
     # ابحث عن IPv4 Address
     # مثال: 192.168.1.100
     ```

2. **افتح من أي جهاز في نفس الشبكة:**
   ```
   http://[IP_ADDRESS]:3001
   ```
   مثال: `http://192.168.1.100:3001`

---

## 👤 الخطوة 6: تسجيل الدخول

### للمستخدمين العاديين (SALES MAN):

**لا يحتاج تسجيل دخول!**
- يمكن البحث مباشرة
- اذهب إلى: `http://[IP]:3001/match`

---

### للمدير (OPERATION MANAGER):

1. **اذهب إلى:**
   ```
   http://[IP]:3001/form
   ```

2. **سجل الدخول:**
   - Username: `admin`
   - Password: `ChangeMe123!`

3. **بعد تسجيل الدخول:**
   - يمكن إضافة منتجات جديدة
   - يمكن تحديث المنتجات
   - يمكن حذف المنتجات

---

## 📱 الخطوة 7: الاستخدام

### للبحث عن منتج (SALES MAN):

1. **افتح:**
   ```
   http://[IP]:3001/match
   ```

2. **املأ المواصفات:**
   - Description: مثل `ATS`
   - Size: مثل `34`
   - Breakers: مثل `CONTACTORS`
   - Brand: مثل `LS`
   - IP Enclosure: مثل `54` (اختياري)
   - Pole: مثل `3P` (اختياري)

3. **اضغط "Match Product"**

4. **النتيجة:**
   - إذا وُجد منتج: سيظهر السعر والرسومات
   - إذا لم يُوجد: ستظهر رسالة "No match found"

---

### لإضافة منتج (OPERATION MANAGER):

1. **افتح:**
   ```
   http://[IP]:3001/form
   ```

2. **سجل الدخول** (إذا لم تكن مسجل دخول)

3. **املأ بيانات المنتج:**
   - جميع الحقول المطلوبة
   - ارفع الرسومات (PDF, JPG, PNG, DWG)

4. **اضغط "Create Product"**

---

## 🛑 الخطوة 8: إيقاف المشروع

### الطريقة السهلة:

1. **في نافذة Command Prompt:**
   - اضغط `Ctrl + C`
   - أو أغلق النافذة

2. **أو من Command Prompt:**
   ```bash
   docker-compose down
   ```

---

## 🔄 الخطوة 9: إعادة التشغيل

### إذا أردت إعادة التشغيل:

1. **شغّل مرة أخرى:**
   ```
   start-local-network.bat
   ```

2. **أو من Command Prompt:**
   ```bash
   docker-compose restart
   ```

---

## 🗄️ الخطوة 10: إدارة قاعدة البيانات

### الوصول إلى Adminer:

1. **افتح:**
   ```
   http://[IP]:8081
   ```

2. **إعدادات الاتصال:**
   - **System:** PostgreSQL
   - **Server:** `db`
   - **Username:** `postgres`
   - **Password:** `postgres`
   - **Database:** `pricing_db`

3. **اضغط "Login"**

4. **الآن يمكنك:**
   - عرض البيانات
   - تعديل البيانات
   - تصدير البيانات
   - استيراد البيانات

---

## ⚙️ الخطوة 11: الإعدادات المتقدمة

### تغيير كلمة المرور:

1. **افتح:**
   ```
   backend\.env
   ```

2. **غيّر:**
   ```env
   ADMIN_USERNAME=admin
   ADMIN_PASSWORD=ChangeMe123!  # ← غيّر هذا
   JWT_SECRET=your-secret-key   # ← غيّر هذا
   ```

3. **أعد تشغيل Backend:**
   ```bash
   docker-compose restart backend
   ```

---

### تغيير Ports (إذا كانت مشغولة):

1. **افتح:**
   ```
   docker-compose.yml
   ```

2. **غيّر Ports:**
   ```yaml
   ports:
     - "3002:3000"  # Frontend (بدلاً من 3001)
     - "4001:4000"  # Backend (بدلاً من 4000)
     - "8082:8080"  # Adminer (بدلاً من 8081)
   ```

3. **أعد تشغيل:**
   ```bash
   docker-compose down
   docker-compose up -d
   ```

---

## 🐛 حل المشاكل الشائعة

### المشكلة 1: Docker Desktop لا يعمل

**الحل:**
1. تأكد أن Docker Desktop مفتوح
2. انتظر حتى يظهر "Docker Desktop is running"
3. أعد تشغيل Docker Desktop

---

### المشكلة 2: Ports مشغولة

**الخطأ:**
```
Error: port 3001 is already in use
```

**الحل:**
1. **اعرف ما يستخدم الـ Port:**
   ```bash
   netstat -ano | findstr :3001
   ```

2. **أوقف العملية:**
   - أو غيّر Port في `docker-compose.yml`

---

### المشكلة 3: لا يمكن الوصول من أجهزة أخرى

**الحل:**
1. تأكد من فتح Firewall:
   ```bash
   # كـ Administrator
   start-local-network-firewall.bat
   ```

2. تأكد من نفس الشبكة:
   - جميع الأجهزة في نفس Wi-Fi/Network

3. تأكد من IP Address:
   ```bash
   ipconfig
   ```

---

### المشكلة 4: المشروع بطيء

**الحل:**
1. **تحقق من Resources:**
   - Docker Desktop → Settings → Resources
   - زد RAM إلى 4GB+
   - زد CPU إلى 2+ cores

2. **أعد تشغيل:**
   ```bash
   docker-compose restart
   ```

---

### المشكلة 5: Database لا يعمل

**الحل:**
1. **تحقق من Logs:**
   ```bash
   docker-compose logs db
   ```

2. **أعد تشغيل Database:**
   ```bash
   docker-compose restart db
   ```

---

## 📊 مراقبة الحالة

### التحقق من حالة الخدمات:

```bash
docker-compose ps
```

**يجب أن ترى:**
```
NAME              STATUS
pricing-db        Up (healthy)
pricing-backend   Up
pricing-frontend  Up
pricing-adminer   Up
```

---

### عرض Logs:

```bash
# جميع Logs
docker-compose logs

# Backend فقط
docker-compose logs backend

# Frontend فقط
docker-compose logs frontend

# Database فقط
docker-compose logs db
```

---

## 💾 النسخ الاحتياطي

### نسخ احتياطي للقاعدة:

```bash
# إنشاء نسخة احتياطية
docker exec pricing-db pg_dump -U postgres pricing_db > backup.sql

# استعادة النسخة الاحتياطية
docker exec -i pricing-db psql -U postgres pricing_db < backup.sql
```

---

## 📝 Checklist سريع

### قبل التشغيل:
- [ ] Docker Desktop مثبت ويعمل
- [ ] المشروع موجود في المجلد الصحيح
- [ ] Ports 3001, 4000, 8081 متاحة

### أثناء التشغيل:
- [ ] شغّل `start-local-network.bat`
- [ ] انتظر حتى "Services Started!"
- [ ] لاحظ IP Address

### بعد التشغيل:
- [ ] افتح Firewall (مرة واحدة)
- [ ] اختبر الوصول: `http://localhost:3001`
- [ ] اختبر من جهاز آخر: `http://[IP]:3001`

---

## 🎯 ملخص سريع

### الخطوات الأساسية:
1. ✅ تثبيت Docker Desktop
2. ✅ فتح مجلد المشروع
3. ✅ شغّل `start-local-network.bat`
4. ✅ افتح Firewall (مرة واحدة)
5. ✅ افتح `http://[IP]:3001`

### الوقت المتوقع:
- **التثبيت الأول:** 10-15 دقيقة
- **التشغيل:** 1-2 دقيقة
- **الوصول:** فوري

---

## 🆘 الدعم

### إذا واجهت مشاكل:

1. **راجع Logs:**
   ```bash
   docker-compose logs
   ```

2. **راجع Health Check:**
   ```
   http://localhost:4000/health
   ```

3. **أعد التشغيل:**
   ```bash
   docker-compose down
   docker-compose up -d
   ```

---

**تم إعداد هذا الدليل بواسطة:** AI Assistant  
**التاريخ:** 19 نوفمبر 2025  
**الحالة:** ✅ **دليل تفصيلي كامل جاهز**

